fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.17
# numpy 1.26 has no prebuilt wheel for Python 3.13 on Windows (pip would compile → needs VS/gcc).
# numpy 2.1+ ships wheels for 3.9–3.13+ on win_amd64.
numpy>=2.1.0,<3
opencv-python-headless==4.10.0.84
# LoFTR deep matcher (optional fallback to SIFT+ORB if import fails). CPU ok; first align downloads weights.
torch>=2.1.0
kornia>=0.7.0
